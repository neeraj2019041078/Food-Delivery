version: "3.8"
services:
  server:
    build: ./server
    ports:
      - "8080:8080"
    env_file:
      - ./server/.env

  client:
    build: ./client
    ports:
      - "3000:80"
    depends_on:
      - server
